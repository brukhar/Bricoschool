%header{:class => "navbar navbar-fixed-top navbar-inverse"}
  .container
    #{link_to "Bricoschool", root_path, id: "logo"}
    %nav
      %ul{:class => "nav navbar-nav navbar-right"}
        %li #{link_to "Annonces", root_path}
        - if connecte?
          %li{:class => "dropdown"}
            %a{:href => "#", :class => "dropdown-toggle", :data => {:toggle => "dropdown"}}
              Compte
              %b.caret
            %ul{:class => "dropdown-menu"}
              %li #{link_to "Profil", utilisateur_courant}
              %li{:class => "divider"}
              %li #{link_to "Se déconnecter", logout_path, method: :delete}
        - else
          %li #{link_to "Se connecter", login_path}
          %li #{link_to "S'inscrire", signup_path}
