%header{:class => "navbar navbar-fixed-top navbar-inverse"}
  .container
    #{link_to "Bricoschool", root_path, id: "logo"}
    %nav
      %ul{:class => "nav navbar-nav navbar-right"}
        %li #{link_to "Annonces", annonces_path}
        %li #{link_to "Professionnels", utilisateurs_path}
        - if connecte?
          %li{:class => "dropdown"}
            %a{:href => "#", :class => "dropdown-toggle", :data => {:toggle => "dropdown"}}
              Compte
              %b.caret
            %ul{:class => "dropdown-menu"}
              %li #{link_to "Profil", utilisateur_courant}
              %li #{link_to "Vos annonces", mes_annonces_path}
              - if pro_connecte?
                %li #{link_to "Vos annonces acceptées", mes_acceptes_path}
              %li{:class => "divider"}
              %li #{link_to "Se déconnecter", logout_path, method: :delete}
        - else
          %li #{link_to "Se connecter", login_path}
          %li #{link_to "S'inscrire", signup_path}
