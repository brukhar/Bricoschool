%h1 Edition d'annonce

.row
  .col-md-6.col-md-offset-3
    = form_for @annonce, url: {action: "update"} do |f|

      %p Titre :
      = f.text_field :titre

      %p Descr :
      = f.text_area :descr, size: "60x12"

      %p Photo :
      = f.file_field :logo

      = f.submit "Sauvegarder les changements", class: "btn btn-primary"
